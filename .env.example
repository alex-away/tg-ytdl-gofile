# Telegram Bot Configuration
BOT_TOKEN=1234567890:XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX  # Get from @BotFather

# User Access Configuration
SUDO_USERS=1234567890,9876543210  # Comma-separated list of admin user IDs with full access
USERS=1111111111,2222222222  # Comma-separated list of normal user IDs (can only use /start, /help, /download)

# Download Configuration
MAX_DOWNLOAD_SIZE=2048  # Maximum download size in MB (default: 2048)
MAX_CONCURRENT_DOWNLOADS=2  # Maximum number of concurrent downloads (default: 2)
FORCE_GOFILE=false  # Force using Gofile for all uploads (testing only)

# Gofile Configuration (Optional)
GOFILE_API_KEY=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx  # Optional: Get from gofile.io for premium features

# Notes:
# 1. SUDO_USERS have access to all commands including user management
# 2. USERS can only use basic commands (start, help, download)
# 3. Users not in either list will be denied access
# 4. All IDs should be numeric values only
# 5. Do not use quotes around values 